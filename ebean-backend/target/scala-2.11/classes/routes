# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

POST    /paper                      controllers.PaperController.paperSearch()

POST    /journal                    controllers.PaperController.journalSearch()

POST    /authoryear                 controllers.PaperController.authorNameYearSearch()

POST    /productiveauthors          controllers.PaperController.productiveAuthors()
